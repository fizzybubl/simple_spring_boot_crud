server:
  servlet:
    context-path: /api/v1
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/disertatie"
    username: root
    password: 1234
  sql:
    init:
      mode: never
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect